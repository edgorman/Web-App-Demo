name: 'GitHub File Diff'
description: 'Determines which parts of the infrastructure and services have changed'

outputs:
  infrastructure-root:
    description: 'Whether infrastructure root files changed'
    value: ${{ steps.filter.outputs.infrastructure-root }}
  infrastructure-prod:
    description: 'Whether infrastructure prod files changed'
    value: ${{ steps.filter.outputs.infrastructure-prod }}
  infrastructure-dev:
    description: 'Whether infrastructure dev files changed'
    value: ${{ steps.filter.outputs.infrastructure-dev }}
  service-backend:
    description: 'Whether backend service files changed'
    value: ${{ steps.filter.outputs.service-backend }}
  service-frontend:
    description: 'Whether frontend service files changed'
    value: ${{ steps.filter.outputs.service-frontend }}

runs:
  using: "composite"
  steps:
    - uses: dorny/paths-filter@v3
      id: filter
      with:
        filters: |
          infrastructure-root:
            - 'infrastructure/config/root/**'
            - 'infrastructure/root/**'
            - '.github/actions/terraform-pull-request/**'
            - '.github/actions/terraform-push-commit/**'
          infrastructure-prod:
            - 'infrastructure/config/prod/**'
            - 'infrastructure/env/**'
            - '.github/actions/terraform-pull-request/**'
            - '.github/actions/terraform-push-commit/**'
          infrastructure-dev:
            - 'infrastructure/config/dev/**'
            - 'infrastructure/env/**'
            - '.github/actions/terraform-pull-request/**'
            - '.github/actions/terraform-push-commit/**'
          service-backend:
            - 'services/backend/**'
            - '.github/actions/backend-pull-request/**'
            - '.github/actions/backend-push-commit/**'
            - '.github/actions/service-push-commit/**'
          service-frontend:
            - 'services/frontend/**'
            - '.github/actions/frontend-push-commit/**'
            - '.github/actions/service-push-commit/**'
