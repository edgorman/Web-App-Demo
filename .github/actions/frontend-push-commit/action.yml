name: 'Frontend Push Commit'
description: 'Builds and deploys the frontend service to Cloud Run'

inputs:
  project_id:
    description: 'GCP Project ID'
    required: true
  region:
    description: 'GCP Region'
    required: true
  service_name:
    description: 'Cloud Run service name'
    required: false
    default: 'frontend'
  workload_identity_provider:
    description: 'The Workload Identity Provider resource name'
    required: true
  service_account:
    description: 'The Google Service Account email'
    required: true

runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/service-push-commit
      with:
        project_id: ${{ inputs.project_id }}
        region: ${{ inputs.region }}
        service_name: ${{ inputs.service_name }}
        service_directory: 'services/frontend'
        repository_name: 'frontend'
        workload_identity_provider: ${{ inputs.workload_identity_provider }}
        service_account: ${{ inputs.service_account }}
