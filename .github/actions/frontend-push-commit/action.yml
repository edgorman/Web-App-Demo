name: 'Frontend Push Commit'
description: 'Builds and deploys the frontend service to Cloud Run with Identity Platform configuration'

inputs:
  project_id:
    description: 'GCP Project ID'
    required: true
  region:
    description: 'GCP Region'
    required: true
  service_name:
    description: 'Cloud Run service name'
    required: false
    default: 'frontend'
  workload_identity_provider:
    description: 'The Workload Identity Provider resource name'
    required: true
  service_account:
    description: 'The Google Service Account email'
    required: true
  google_oauth_client_id:
    description: 'Google OAuth Client ID for Identity Platform'
    required: true
  identity_platform_api_key:
    description: 'Identity Platform API Key from Secret Manager'
    required: true
  backend_url:
    description: 'Backend Cloud Run service URL'
    required: true

runs:
  using: "composite"
  steps:
    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: ${{ inputs.workload_identity_provider }}
        service_account: ${{ inputs.service_account }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Create .env file
      shell: bash
      working-directory: services/frontend
      run: |
        cat > .env <<EOF
        ENV_PROJECT_ID=${{ inputs.project_id }}
        ENV_API_KEY=${{ inputs.identity_platform_api_key }}
        ENV_CLIENT_ID=${{ inputs.google_oauth_client_id }}
        ENV_BACKEND_URL=${{ inputs.backend_url }}
        EOF

    - name: Configure Docker for Artifact Registry
      shell: bash
      run: |
        gcloud auth configure-docker ${{ inputs.region }}-docker.pkg.dev

    - name: Build Docker image
      shell: bash
      working-directory: services/frontend
      env:
        BUILDX_NO_DEFAULT_ATTESTATIONS: 1
      run: |
        # Build and tag image for the frontend repository in Artifact Registry
        docker build -t ${{ inputs.region }}-docker.pkg.dev/${{ inputs.project_id }}/frontend/frontend:${{ github.sha }} \
                     -t ${{ inputs.region }}-docker.pkg.dev/${{ inputs.project_id }}/frontend/frontend:latest .

    - name: Push Docker image
      shell: bash
      run: |
        docker push --all-tags ${{ inputs.region }}-docker.pkg.dev/${{ inputs.project_id }}/frontend/frontend

    - name: Deploy to Cloud Run
      shell: bash
      run: |
        gcloud run deploy ${{ inputs.service_name }} \
          --image=${{ inputs.region }}-docker.pkg.dev/${{ inputs.project_id }}/frontend/frontend:${{ github.sha }} \
          --region=${{ inputs.region }} \
          --project=${{ inputs.project_id }} \
          --platform=managed
